Apply the updated visual styles from the new design frame.

Use the new frame as the source of truth for styling.

Do NOT redesign components manually.  
Instead synchronize styles with the new layout.

---

1. PRIMARY LESSON CARD STYLE

The main light lesson card (example: "Цвет") has been visually updated.

Update the card to match the new design:

• background tone slightly adjusted  
• updated shadows  
• updated internal spacing  
• improved visual hierarchy

Replace the current implementation with the styles from the new frame.

---

2. THEORY BUTTON STYLE

The "Теория" button inside the lesson card has changed.

Update it to match the new design:

• slightly lighter background  
• updated contrast  
• same size and layout

Use the new button style from the frame.

---

3. CONTINUE BUTTON STYLE

The "Продолжить" button has also been updated.

Use the exact style from the new frame:

• updated shadow  
• updated press animation  
• updated color balance

Replace the existing button styling with the new one.

---

4. ROADMAP VISUAL UPDATE

The roadmap path on the right side has a new gradient style.

Update the roadmap path to match the new gradient.

Also ensure the active step glow matches the new frame.

---

5. ROADMAP STEP NUMBERS

Each roadmap step now includes a number indicator.

Add step numbers next to the nodes:

01  
02  
03  
04

These numbers should be subtle and sit near the nodes.

---

6. ADD HOMEWORK STEP

Add a new roadmap node for:

Домашнее задание

The learning path order becomes:

01 Транспорт  
02 Цвет  
03 Иерархия  
04 Домашнее задание

Use the same circular node style.

The homework step should have its own icon.

---

7. SIDEBAR FEEDBACK BLOCK

Add the feedback block to the sidebar.

Content:

Оставь отзыв о UXEO  
И получи месяц бесплатно

Include:

• gift icon  
• button "Написать отзыв"

Match the style from the new frame.

---

Important rule:

Do not modify layout structure.

Only synchronize styles and new elements from the updated design frame.