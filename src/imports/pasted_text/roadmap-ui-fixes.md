ROADMAP UI FIXES

Apply the following UI fixes to the roadmap screen.

IMPORTANT RULES

- Do NOT redesign the layout.
- Do NOT change spacing system globally.
- Do NOT change typography scale.
- Do NOT modify sidebar or widget column.
- Only apply the specific fixes listed below.



FIX 1 — CONTINUE BUTTON SIZE

The orange "Продолжить" button in the widget column is visually too dominant.

Adjust the button width so it becomes more balanced with the right column widgets.

Rules:

• reduce width by approximately 20–25%  
• keep the same height  
• keep the same color  
• keep the same shadow and press animation  
• keep icon + XP badge



FIX 2 — ORANGE COLOR UNIFICATION

The orange used for:

• current lesson node  
• "Продолжить" button

must use the same color token.

Ensure both use the exact same orange value.

Do NOT introduce a new color.



FIX 3 — MOVE "ТЫ ЗДЕСЬ" LABEL

The handwritten label "Ты здесь" currently sits next to the active node.

Move this label upward so it sits between the nodes:

Контраст  
Цвет

The label should visually point toward the current node.

Vertical shift roughly: ~200px upward.

Keep the same style and rotation.



FIX 4 — MODULE DIVIDER POSITION

The dotted divider line that separates modules is incorrectly placed.

Move the divider so it sits between:

Иерархия  
Домашнее задание

The divider must visually indicate the start of the next module.



FIX 5 — MODULE TITLES ALIGNMENT

Module labels:

"Модуль 1"  
"Модуль 2"

must align with the main roadmap container left edge.

Align them with the left boundary of the roadmap content area (same vertical axis as the header arrow and content container).



FIX 6 — RESTORE MISSING HANDWRITTEN SVG

There is a missing handwritten SVG annotation near the "Домашнее задание" node.

Restore it.

Rules:

• position slightly to the left of the node  
• visually underline or highlight the homework node  
• maintain handwritten style  
• do not change its graphic style



GOAL

Apply these fixes without redesigning the roadmap.
The overall visual structure must remain identical.