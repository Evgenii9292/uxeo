Обновить ТОЛЬКО текст в квизах (без изменения структуры).

---

ВАЖНО:

НЕЛЬЗЯ менять:
- ключи объектов
- структуру данных
- типы вопросов
- массивы (options, compareOptions, pairs)
- previewType
- content
- любые поля кроме текста

---

МОЖНО менять ТОЛЬКО:

- question (текст вопроса)
- text внутри options
- explanation
- label (если это текст, не логика)

---

ПРИМЕР:

Было:
{
  type: "single_choice",
  question: "Старый текст",
  options: [
    { label: "А", text: "Старый вариант" }
  ]
}

Стало:
{
  type: "single_choice",
  question: "Новый текст",
  options: [
    { label: "А", text: "Новый вариант" }
  ]
}

---

ЗАПРЕЩЕНО:

❌ менять options → answers  
❌ менять compareOptions → options  
❌ удалять previewType  
❌ менять структуру объектов  
❌ создавать новые квизы  
❌ удалять поля  

---

ОСОБЕННО ДЛЯ compare_ui:

Структура ДОЛЖНА остаться:

compareOptions: [
  { label: "А", previewType: "bad" },
  { label: "Б", previewType: "good" }
]

Менять можно только description / подписи, если они есть.

---

MATCHING:

- верх (визуал) НЕ ТРОГАТЬ
- менять только текст снизу

---

ЗАДАЧА:

Я дам новый текст.

Твоя задача:
👉 заменить ТОЛЬКО строки внутри существующих полей  
👉 ничего больше не трогать

---

ПРОВЕРКА:

После изменений:
- все варианты ответов должны отображаться
- ничего не должно пропасть
- структура должна быть идентична исходной

---

ЕСЛИ СОМНЕВАЕШЬСЯ:

НЕ ТРОГАЙ поле.