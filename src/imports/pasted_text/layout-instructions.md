You ignored most of the previous instructions. Please apply ALL the following corrections.

This is not optional. Implement every item below.

-------------------------------------

1. HEADER HEIGHT

Reduce header height by about 30% on all layouts.

Desktop target height: ~70px.

Everything inside the header must stay vertically centered.

-------------------------------------

2. TYPOGRAPHY

Remove bold and semi-bold everywhere on this screen.

Use Medium as the maximum font weight.

This applies to:
- titles
- accordion headers
- labels
- buttons

Right now the main title is still bold. Fix it.

-------------------------------------

3. DESKTOP LAYOUT STRUCTURE

Layout must consist of:

LEFT: Sidebar (fixed width)

CENTER: Main content (flexible)

RIGHT: Widgets column (fixed)

The right widgets must stay pinned to the right side.

Only the center content area should stretch or shrink.

Spacing between center content and right widgets must always be 20px.

-------------------------------------

4. BREAKPOINT BEHAVIOR (<1440px)

When screen width becomes smaller than 1440px:

Only the center content area should shrink.

Do NOT shrink the right widgets.

Keep 20px spacing between center content and widgets.

-------------------------------------

5. TABLET LAYOUT

Tablet begins at 1280px.

Tablet layout must be centered, not attached to the left side.

Currently the spacing is uneven.

Make equal 20px margins from left and right screen edges.

-------------------------------------

6. MOBILE VERSION – LARGE GAP

On mobile there is a large empty gap between the last accordion and the orange button.

Remove it.

Spacing should be ~20px only.

-------------------------------------

7. MOBILE FLOATING CIRCLES

The floating circles on the right side appear oval instead of circular.

Fix them so they are perfect circles.

Spacing between circles: 20px.

-------------------------------------

8. HEADER SHADOW

There is a grey shadow/gradient above the hero area.

Remove it.

The background should match the page background.

-------------------------------------

9. HEADER SIZE ON MOBILE

Header is too large.

Reduce height proportionally (same ~30% reduction).

-------------------------------------

10. QUIZ CARDS

Ensure the quiz interface inside cards has ~20px internal padding on all sides.

Right now the interface fills too much space.

-------------------------------------

This layout container will become the base layout system for the entire project.

Please implement these fixes exactly as described.